machine:
  python:
    version: 3.5.1

dependencies:
  cache_directories:
    - .tox

  override:
    - pip install -U bootstrapper pip wheel
    - make install setup-pyenv

  pre:
    - sudo apt-get install -y language-pack-uk

test:
  override:
    - make test

  post:
    - "coveralls || :"

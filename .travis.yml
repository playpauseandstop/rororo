sudo: false
addons:
  apt:
    packages:
      - language-pack-en
      - language-pack-uk

language: python
python:
  - 3.5
env:
  - TOXENV=py34
  - TOXENV=py35

install:
  - pip install coveralls==1.1 tox==2.2.1
script:
  - tox
  - coveralls
